# OS generated files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# IDEs
.vscode/
.idea/
*.swp
*.swo

# AI assistant config (except project tracking files)
.claude/settings.local.json
!.claude/PROJECT_INDEX.md
!.claude/MEMORY.md
!.claude/.gitkeep

# Project specific
logs/
*.log
# Anchor to repo root so it doesn't match tools/sandbox/ (the CLI).
/sandbox/

# Godot engine
.godot/
export_presets.cfg
.claude/worktrees/

# Blender autobackup files
*.blend1
*.blend2

# Cutscene pipeline — intermediate renders are regenerable; only the final
# composited PNG sequences under godot/assets/cutscenes/ are shipped.
/cutscene_pipeline/renders/
/cutscene_pipeline/blender/*.blend
/cutscene_pipeline/blender/__pycache__/

# Large external tooling / asset libraries (kept local, not in repo)
tools/Universal Animation Library*
tools/blender-dev/
/cutscene_pipeline/archives/
